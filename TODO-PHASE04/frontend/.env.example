# Environment Variables Template
# Copy this file to .env.local and fill in your values for local development overrides

# Backend API Configuration
# For local development, use: http://localhost:8000
# For production, use: https://sobiarao-todo-apps.hf.space
NEXT_PUBLIC_API_URL=http://localhost:8000

# Better Auth Configuration (if needed)
# Should match the API URL
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:8000

# Environment
# Options: development | production
NEXT_PUBLIC_ENVIRONMENT=development

# ============================================
# NOTES:
# ============================================
# - .env.development is loaded automatically when running: npm run dev
# - .env.production is loaded automatically when running: npm run build
# - .env.local overrides all other env files (use for personal settings)
# - .env.local is NOT committed to git
# - For Vercel deployment, set environment variables in the Vercel dashboard
